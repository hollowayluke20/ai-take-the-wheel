# database/

One JSON file per analysis run (e.g. `2026-09-08-my-project.json`).

Each file records the input (repo URL or idea text), the per-problem ranked
options, and the evidence snapshots used for ranking.
