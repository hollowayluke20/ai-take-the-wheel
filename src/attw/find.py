"""Find stage: find existing solutions for each problem elsewhere."""


def search(problem: str) -> list[str]:
    """Stub: return a fixture query regardless of problem (real logic TBD)."""
    return [f"existing solutions for {problem}"]
