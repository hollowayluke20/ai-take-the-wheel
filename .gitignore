__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
*.jsonl
.DS_Store

# attw weekly mention cache — refreshable, not committed (ticket 09).
database/cache/

# attw per-run records — proof trail lives in answer-keys + RUNLOG + map (C10).
database/*
!database/README.md

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
